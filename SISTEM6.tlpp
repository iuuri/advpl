#include "Protheus.ch"
#include "Totvs.ch"

User Function SYSTEM6()
Local lSol as logical
Local lPiscina as logical

lSol      := .F.
lPiscina  := .F.

IF lSol == .T. .OR. lPiscina
  MsgInfo("Partiu Praia", "Resultado")
ELSE 
  MsgInfo("Ficar em casa", "Resultado")
ENDIF


return
